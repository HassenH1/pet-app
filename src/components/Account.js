import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Account = () => {
  return (
    <View>
      <Text>Testing for tab navigator</Text>
    </View>
  );
};

export default Account;

const styles = StyleSheet.create({});
